spring:
  application:
    name: Userprovider
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG_SERVER
      profile: test
      label: master
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka,http://localhost:8889/eureka

